<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:foregroundGravity="center_vertical"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        tools:src="@android:drawable/ic_dialog_info" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="15dp"
        android:paddingLeft="5dp"
        android:paddingEnd="15dp"
        android:paddingRight="5dp">

        <TextView
            android:id="@+id/settingName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@android:style/TextAppearance.Holo.Large"
            tools:text="Name of setting which might be short or long. This should never be truncated--it should wrap onto as many lines as needed." />

        <TextView
            android:id="@+id/settingDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Description of setting which can be rather long. Perhaps it can wrap onto another line. In any case, this whole thing should be displayed to the user." />
    </LinearLayout>
</LinearLayout>